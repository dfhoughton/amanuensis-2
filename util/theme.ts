import { createTheme } from "@mui/material"

export const theme = createTheme({
  palette: {
    primary: {
      main: "#721817",
    },
    secondary: {
      main: "#fa9f42",
    },
  },
  typography: {
    fontFamily: "fantasy"
  }
})
