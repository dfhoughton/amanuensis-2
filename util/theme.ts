import { createTheme } from "@mui/material"

export const theme = createTheme({
  palette: {
    primary: {
      main: "#931f1d",
    },
    secondary: {
      main: "#937b63",
    },
  },
  typography: {
    fontFamily: "Helvetica"
  }
})
